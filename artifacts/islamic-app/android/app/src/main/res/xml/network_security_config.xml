<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security config for Noor Quran.
    Only HTTPS is allowed; no cleartext traffic.
    Trusted APIs: alquran.cloud, aladhan.com, cdn.islamic.network, everyayah.com
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow the Capacitor local server (always HTTPS via androidScheme=https) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">localhost</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
