<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme — dark green, no action bar -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">#1a5c38</item>
        <item name="colorPrimaryDark">#071a0e</item>
        <item name="colorAccent">#1a5c38</item>
        <item name="android:windowBackground">#071a0e</item>
        <item name="android:statusBarColor">#071a0e</item>
        <item name="android:navigationBarColor">#071a0e</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Activity content theme — no action bar, draws system bar backgrounds -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Launch/splash theme: shown during cold start before WebView is ready.
         Parent is Theme.SplashScreen so androidx.core.splashscreen.SplashScreen
         .installSplashScreen() can read windowSplashScreen* attributes correctly
         on Android 12+ (API 31+) and fall back gracefully on older APIs.
         postSplashScreenTheme switches the window to AppTheme.NoActionBar once
         the splash finishes — BridgeActivity also does this programmatically. -->
    <style name="AppTheme.Launcher" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="colorPrimary">#1a5c38</item>
        <item name="colorPrimaryDark">#071a0e</item>
        <item name="colorAccent">#1a5c38</item>
    </style>
</resources>
