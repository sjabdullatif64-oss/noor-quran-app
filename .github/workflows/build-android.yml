##############################################################################
# SUPERSEDED — This workflow is disabled.
#
# The canonical Android build pipeline is:
#   .github/workflows/android-build.yml   ← use this one
#
# That workflow uses Capacitor (native Android shell) and produces a proper
# signed APK + AAB compatible with your existing Play Store listing.
#
# This file used Bubblewrap/TWA which is a different, incompatible approach.
##############################################################################

name: "[DISABLED] Old TWA Build — use android-build.yml"

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "⚠️ This workflow is disabled. Use android-build.yml instead."
        required: true
        default: "use android-build.yml"

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow is superseded
        run: |
          echo "❌ This workflow (Bubblewrap/TWA) is superseded and disabled."
          echo ""
          echo "✅ Use the correct pipeline instead:"
          echo "   Actions → 'Noor Quran — Android Build'"
          echo "   File: .github/workflows/android-build.yml"
          echo ""
          echo "   That workflow builds a real Capacitor native Android app"
          echo "   (signed APK + AAB) using your existing Play Store keystore."
          exit 1
